{{> navbar-partial user = 'user'}}
<br><br><br>
<main>
    <div class="container">
        <div class="row">
            {{#each days}}
                {{> day-partial}}
            {{/each}}
        </div>
    </div>
</main>
<br><br>
{{> footer-partial}}